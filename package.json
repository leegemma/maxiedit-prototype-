{
  "name": "maxiedit",
  "version": "0.1.0",
  "description": "MaxiEdit Android app — Capacitor wrapper around the web prototype",
  "private": true,
  "scripts": {
    "sync:www": "rm -rf www && mkdir www && cp index.html www/",
    "cap:add:android": "npm run sync:www && npx cap add android",
    "cap:sync": "npm run sync:www && npx cap sync",
    "cap:open:android": "npx cap open android",
    "cap:run:android": "npm run cap:sync && npx cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
