.DS_Store
.claude/settings.local.json
new

# Capacitor / Node
node_modules/
www/
package-lock.json

# Android build outputs
android/build/
android/app/build/
android/.gradle/
android/local.properties
android/app/release/
android/captures/
*.apk
*.aab

# Android signing — never commit keystore or its credentials
*.keystore
*.jks
keystore.properties
key.properties

# iOS build outputs
ios/App/Pods/
ios/App/App/public/
ios/App/App/capacitor.config.json
# Auto-generated bridge for Cordova plugins. cap sync regenerates this
# from node_modules (with patches re-applied via patch-package), so
# tracking it would just produce spurious diffs.
ios/capacitor-cordova-ios-plugins/
*.xcuserstate
xcuserdata/
